
incoming data:

URL
title
context

run url-update which will create or update the url based on the data

have a list of hosts that never get added to the system.
if a host gets added to the list then existing urls shoudl be marked to purged

an url is an id so it should be stored as such, the base url should be stored as
a container id. also the url with no parameters should be added for reference.

create a list of hostnames where parameters are part of the identity of the
resource the url points to; possibly another list where patameters should
be ignored at all times.
